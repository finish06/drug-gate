basePath: /
definitions:
  github_com_finish06_drug-gate_internal_apikey.APIKey:
    properties:
      active:
        type: boolean
      app_name:
        type: string
      created_at:
        type: string
      expires_at:
        type: string
      key:
        type: string
      origins:
        items:
          type: string
        type: array
      rate_limit:
        type: integer
    type: object
  github_com_finish06_drug-gate_internal_model.DrugClass:
    properties:
      name:
        type: string
      type:
        type: string
    type: object
  github_com_finish06_drug-gate_internal_model.DrugClassResponse:
    properties:
      brand_names:
        items:
          type: string
        type: array
      classes:
        items:
          $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.DrugClass'
        type: array
      generic_name:
        type: string
      query_name:
        type: string
    type: object
  github_com_finish06_drug-gate_internal_model.DrugDetailResponse:
    properties:
      classes:
        items:
          type: string
        type: array
      generic_name:
        type: string
      name:
        type: string
      ndc:
        type: string
    type: object
  github_com_finish06_drug-gate_internal_model.ErrorResponse:
    properties:
      error:
        type: string
      message:
        type: string
    type: object
  github_com_finish06_drug-gate_internal_model.PaginatedResponse:
    properties:
      data: {}
      pagination:
        $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.Pagination'
    type: object
  github_com_finish06_drug-gate_internal_model.Pagination:
    properties:
      limit:
        type: integer
      page:
        type: integer
      total:
        type: integer
      total_pages:
        type: integer
    type: object
  github_com_finish06_drug-gate_internal_model.RxNormCandidate:
    properties:
      name:
        type: string
      rxcui:
        type: string
      score:
        type: integer
    type: object
  github_com_finish06_drug-gate_internal_model.RxNormConcept:
    properties:
      name:
        type: string
      rxcui:
        type: string
    type: object
  github_com_finish06_drug-gate_internal_model.RxNormGenericResponse:
    properties:
      generics:
        items:
          $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.RxNormConcept'
        type: array
      rxcui:
        type: string
    type: object
  github_com_finish06_drug-gate_internal_model.RxNormNDCResponse:
    properties:
      ndcs:
        items:
          type: string
        type: array
      rxcui:
        type: string
    type: object
  github_com_finish06_drug-gate_internal_model.RxNormProfile:
    properties:
      brand_names:
        items:
          type: string
        type: array
      generic:
        $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.RxNormConcept'
      name:
        type: string
      ndcs:
        items:
          type: string
        type: array
      query:
        type: string
      related:
        $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.RxNormRelatedResponse'
      rxcui:
        type: string
    type: object
  github_com_finish06_drug-gate_internal_model.RxNormRelatedResponse:
    properties:
      brand_names:
        items:
          $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.RxNormConcept'
        type: array
      branded_drugs:
        items:
          $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.RxNormConcept'
        type: array
      clinical_drugs:
        items:
          $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.RxNormConcept'
        type: array
      dose_forms:
        items:
          $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.RxNormConcept'
        type: array
      ingredients:
        items:
          $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.RxNormConcept'
        type: array
      rxcui:
        type: string
    type: object
  github_com_finish06_drug-gate_internal_model.RxNormSearchResult:
    properties:
      candidates:
        items:
          $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.RxNormCandidate'
        type: array
      query:
        type: string
      suggestions:
        items:
          type: string
        type: array
    type: object
  internal_handler.cacheClearResult:
    properties:
      keys_deleted:
        type: integer
      status:
        type: string
    type: object
  internal_handler.createKeyRequest:
    properties:
      app_name:
        type: string
      origins:
        items:
          type: string
        type: array
      rate_limit:
        type: integer
    type: object
  internal_handler.rotateKeyRequest:
    properties:
      grace_period:
        type: string
    type: object
  internal_handler.rotateKeyResponse:
    properties:
      new_key:
        type: string
      old_key:
        type: string
      old_key_expires_at:
        type: string
    type: object
host: localhost:8081
info:
  contact: {}
  description: Drug information gateway — NDC lookup, therapeutic classes, and more.
  title: drug-gate API
  version: 0.1.0
paths:
  /admin/cache:
    delete:
      description: Deletes all cache keys matching cache:*, or a subset matching cache:{prefix}*
        if the prefix query parameter is provided. Does not affect API key or rate
        limit data.
      parameters:
      - description: Key prefix filter (e.g. rxnorm, drugnames)
        in: query
        name: prefix
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/internal_handler.cacheClearResult'
        "502":
          description: Redis unavailable
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.ErrorResponse'
      summary: Clear Redis cache
      tags:
      - admin
  /admin/keys:
    get:
      description: Returns all provisioned API keys with metadata.
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            items:
              $ref: '#/definitions/github_com_finish06_drug-gate_internal_apikey.APIKey'
            type: array
        "500":
          description: Internal error
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.ErrorResponse'
      summary: List all API keys
      tags:
      - admin
    post:
      consumes:
      - application/json
      description: Provisions a new publishable API key with app name, allowed origins,
        and rate limit.
      parameters:
      - description: Key creation parameters
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/internal_handler.createKeyRequest'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_apikey.APIKey'
        "400":
          description: Invalid request body or validation error
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.ErrorResponse'
        "500":
          description: Internal error
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.ErrorResponse'
      summary: Create API key
      tags:
      - admin
  /admin/keys/{key}:
    delete:
      description: Marks an API key as inactive. The key remains retrievable but will
        be rejected by auth middleware.
      parameters:
      - description: API key to deactivate
        in: path
        name: key
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 'status: deactivated'
          schema:
            additionalProperties:
              type: string
            type: object
        "500":
          description: Internal error
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.ErrorResponse'
      summary: Deactivate API key
      tags:
      - admin
    get:
      description: Returns metadata for a specific API key.
      parameters:
      - description: API key (e.g. pk_abc123)
        in: path
        name: key
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_apikey.APIKey'
        "404":
          description: Key not found
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.ErrorResponse'
        "500":
          description: Internal error
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.ErrorResponse'
      summary: Get API key details
      tags:
      - admin
  /admin/keys/{key}/rotate:
    post:
      consumes:
      - application/json
      description: Creates a new key with the same metadata and sets a grace period
        expiration on the old key. Both keys are valid during the grace period.
      parameters:
      - description: API key to rotate
        in: path
        name: key
        required: true
        type: string
      - description: Grace period for old key
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/internal_handler.rotateKeyRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/internal_handler.rotateKeyResponse'
        "400":
          description: Invalid request or grace period
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.ErrorResponse'
        "500":
          description: Internal error
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.ErrorResponse'
      summary: Rotate API key
      tags:
      - admin
  /health:
    get:
      description: Returns service health status and build version.
      produces:
      - application/json
      responses:
        "200":
          description: status and version
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Health check
      tags:
      - system
  /openapi.json:
    get:
      description: Returns the generated OpenAPI specification as JSON.
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: OpenAPI spec
      tags:
      - system
  /v1/drugs/class:
    get:
      description: Looks up a drug by generic or brand name and returns its pharmacological
        classes, brand names, and generic name. Tries generic name first, falls back
        to brand name.
      parameters:
      - description: Drug name (generic or brand, e.g. simvastatin, Zocor)
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.DrugClassResponse'
        "400":
          description: Missing or empty name parameter
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.ErrorResponse'
        "404":
          description: Drug not found
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.ErrorResponse'
        "502":
          description: Upstream service error
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.ErrorResponse'
      summary: Look up drug class by name
      tags:
      - drugs
  /v1/drugs/classes:
    get:
      description: 'Returns a paginated list of pharmacological drug classes from
        DailyMed. Defaults to EPC (Established Pharmacologic Class) type. Supports
        filtering by type: epc, moa, pe, cs, or all.'
      parameters:
      - description: 'Filter by class type (default: epc)'
        enum:
        - epc
        - moa
        - pe
        - cs
        - all
        in: query
        name: type
        type: string
      - description: 'Page number (default: 1)'
        in: query
        name: page
        type: integer
      - description: 'Results per page (default: 50, max: 100)'
        in: query
        name: limit
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.PaginatedResponse'
        "502":
          description: Upstream service error
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.ErrorResponse'
      summary: List drug classes
      tags:
      - drugs
  /v1/drugs/classes/drugs:
    get:
      description: Returns a paginated list of drugs belonging to a specific pharmacological
        class, resolved via FDA NDC data. Returns empty data (not 404) for unknown
        classes.
      parameters:
      - description: Pharmacological class name (e.g. HMG-CoA Reductase Inhibitor)
        in: query
        name: class
        required: true
        type: string
      - description: 'Page number (default: 1)'
        in: query
        name: page
        type: integer
      - description: 'Results per page (default: 100, max: 500)'
        in: query
        name: limit
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.PaginatedResponse'
        "400":
          description: Missing or empty class parameter
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.ErrorResponse'
        "502":
          description: Upstream service error
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.ErrorResponse'
      summary: List drugs in a class
      tags:
      - drugs
  /v1/drugs/names:
    get:
      description: Returns a paginated list of drug names from the DailyMed dataset.
        Supports case-insensitive substring search and type filtering (generic/brand).
      parameters:
      - description: Search substring filter (case-insensitive)
        in: query
        name: q
        type: string
      - description: 'Filter by type: generic, brand, or all'
        enum:
        - generic
        - brand
        - all
        in: query
        name: type
        type: string
      - description: 'Page number (default: 1)'
        in: query
        name: page
        type: integer
      - description: 'Results per page (default: 50, max: 100)'
        in: query
        name: limit
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.PaginatedResponse'
        "502":
          description: Upstream service error
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.ErrorResponse'
      summary: List drug names
      tags:
      - drugs
  /v1/drugs/ndc/{ndc}:
    get:
      description: Accepts a product NDC (dash-separated), queries cash-drugs upstream,
        and returns drug name, generic name, and therapeutic classes. Supports 5-4,
        4-4, and 5-3 formats with automatic fallback to padded 5-4.
      parameters:
      - description: Product NDC with dash (e.g. 58151-158, 0069-3150, 00069-315)
        in: path
        name: ndc
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.DrugDetailResponse'
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.ErrorResponse'
        "404":
          description: Not Found
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.ErrorResponse'
        "502":
          description: Bad Gateway
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.ErrorResponse'
      summary: Look up drug by NDC
      tags:
      - drugs
  /v1/drugs/rxnorm/{rxcui}/generics:
    get:
      description: Returns generic product information for the given RxNorm concept
        identifier.
      parameters:
      - description: RxNorm concept unique identifier
        in: path
        name: rxcui
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.RxNormGenericResponse'
        "404":
          description: RxCUI not found
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.ErrorResponse'
        "502":
          description: Upstream service error
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.ErrorResponse'
      summary: Get generic products for an RxCUI
      tags:
      - rxnorm
  /v1/drugs/rxnorm/{rxcui}/ndcs:
    get:
      description: Returns NDC codes associated with the given RxNorm concept identifier.
      parameters:
      - description: RxNorm concept unique identifier
        in: path
        name: rxcui
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.RxNormNDCResponse'
        "404":
          description: RxCUI not found
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.ErrorResponse'
        "502":
          description: Upstream service error
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.ErrorResponse'
      summary: Get NDCs for an RxCUI
      tags:
      - rxnorm
  /v1/drugs/rxnorm/{rxcui}/related:
    get:
      description: Returns all related concepts grouped by type (ingredients, brand
        names, dose forms, clinical drugs, branded drugs).
      parameters:
      - description: RxNorm concept unique identifier
        in: path
        name: rxcui
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.RxNormRelatedResponse'
        "404":
          description: RxCUI not found
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.ErrorResponse'
        "502":
          description: Upstream service error
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.ErrorResponse'
      summary: Get related concepts for an RxCUI
      tags:
      - rxnorm
  /v1/drugs/rxnorm/profile:
    get:
      description: Resolves a drug name via approximate match, then assembles NDCs,
        generic equivalents, and related concepts into a single response.
      parameters:
      - description: Drug name (generic or brand)
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.RxNormProfile'
        "400":
          description: Missing name parameter
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.ErrorResponse'
        "404":
          description: Drug not found
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.ErrorResponse'
        "502":
          description: Upstream service error
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.ErrorResponse'
      summary: Get unified drug profile
      tags:
      - rxnorm
  /v1/drugs/rxnorm/search:
    get:
      description: Approximate match search via RxNorm. Returns up to 5 candidates
        ranked by score. Includes spelling suggestions when no matches are found.
      parameters:
      - description: Drug name to search for
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.RxNormSearchResult'
        "400":
          description: Missing name parameter
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.ErrorResponse'
        "404":
          description: No drugs found
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.ErrorResponse'
        "502":
          description: Upstream service error
          schema:
            $ref: '#/definitions/github_com_finish06_drug-gate_internal_model.ErrorResponse'
      summary: Search drugs by name (RxNorm)
      tags:
      - rxnorm
  /version:
    get:
      description: Returns build version, git commit, git branch, and Go runtime version.
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Build version info
      tags:
      - public
swagger: "2.0"
